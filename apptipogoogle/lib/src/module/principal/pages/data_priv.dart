import 'package:flutter/material.dart';

class DataPrivPage extends StatelessWidget {
  const DataPrivPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Espacio para los Datos y Privacidad'));
  }
}