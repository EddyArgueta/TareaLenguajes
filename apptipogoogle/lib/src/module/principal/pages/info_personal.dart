import 'package:flutter/material.dart';

class InfoPersonalPage extends StatelessWidget {
  const InfoPersonalPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Información Personal :D'));
  }
}